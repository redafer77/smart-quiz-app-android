<div dir="rtl">

# سجل التغييرات

## غير مُصدَر

- **توازن أطوال الخيارات:** كانت الإجابة الصحيحة في أسئلة التعريفات جملة شارحة
  والمشتّتات كلمتين، فصار طول الخيار يكشف الصواب: عند وجود خيار أطول من ثانيه
  بنسبة 30% كان هو الصحيح في 52% من الأسئلة بدل 25% المتوقعة بالصدفة، أي أن
  لاعباً لا يعرف شيئاً يتفوق على التخمين. أُعيدت كتابة مشتّتات 112 سؤالاً بطول
  مقارب ومضمون معقول، فهبطت النسبة إلى 27% (وإلى 23% عند فارق 50%، و12% عند 80%).
- ثلاثة أسئلة كانت معيبة الصياغة أو الإجابة أُعيدت كتابتها، منها سؤال عن أول عربي
  فاز بذهبية أولمبية في الماراثون كانت إجابته غامضة — والصحيح أحمد بوقرة الوافي
  الجزائري في أمستردام 1928.
- `tools/build_bank.py` يقيس انحياز الطول ويفشل إن تجاوز 34%، و`tools/balance_distractors.py`
  يحفظ المشتّتات المكتوبة. الـCI يتحقق أيضاً أن `questions.json` مطابق لملفات المصدر.

## 1.4

- **فحص جودة البنك:** كاشف يرفض الأسئلة التي يظهر جوابها في نصّها، ومتحقّق حسابي يعيد حساب كل سؤال رياضي قابل للحساب (49 سؤالاً) ويقارنه بالجواب المخزَّن.
- صُحّح 16 سؤالاً كشفها الفحص، أسوأها `ما وحدة القياس الأكبر: الجيجابايت أم التيرابايت؟` — كان نصّه يعرض خيارَين بينما له أربعة، و`ما اسم أسلوب الشرط في جملة إن تدرس تنجح؟` الذي جوابه «شرط».
- `tools/build_bank.py` صار بوابة: يفشل بخروج 1 عند أي عيب، ولا يكتب `questions.json` إلا بعد نجاح كل الفحوص، فلا يدهس ملفاً سليماً.
- CI يتحقق أن `data/questions.json` يطابق `data/source` فلا ينحرف المولَّد عن مصدره.

## 1.3

- **مراجعة الأخطاء:** كل سؤال يُخطئ فيه المستخدم يُحفظ في قائمة مراجعة، وزر «مراجعة أخطائي» يبني منها اختباراً. الإجابة الصحيحة تحذف السؤال من القائمة، فتتقلّص مع التحسّن. المراجعة لا تُلوّث سجل النتائج.
- **أداء الفئات:** شاشة السجل صار فيها عرض ثانٍ يلخّص نسبة الإتقان في كل فئة مرتّبةً من الأضعف إلى الأقوى.
- فاحص تغطية الخط منع أول محاولة لرسم أشرطة الأداء بمحارف `█░` غير الموجودة في Amiri.
- سير عمل يدوي لبناء نسخة release موقّعة صالحة للمتاجر، وتوضيح الفرق عن إعادة توقيع نسخة debug.

## 1.2

- بنك أسئلة مضمَّن من **1000 سؤال** في 10 فئات (عامة، جغرافيا، تاريخ، علوم، رياضيات، إسلاميات، لغة عربية، تقنية، رياضة، أدب وفنون) و4 مستويات صعوبة.
- **إصلاح قابلية التخمين:** كانت 86% من الإجابات الصحيحة في الخيار الثاني. صار `tools/build_bank.py` يوزّع مواضع الإجابات بإزاحة دائرية حتمية (250 لكل موضع)، مع اختبار يحرس التوازن.
- شاشة اختيار الفئة والمستوى وعدد الأسئلة قبل بدء الاختبار.
- بحث وترقيم صفحات في إدارة الأسئلة حتى لا تبطؤ القوائم الطويلة.
- استيراد الأسئلة من ملف نصي وتصديرها، مع استعادة البنك الافتراضي.
- تصنيف الأسئلة المضافة يدوياً وتخزين الفئة والمستوى في سجل النتائج.
- **إصلاح التفاف الأسطر:** كان Kivy يلفّ النص بعد تحويله للترتيب البصري فيقلب ترتيب الأسطر (آخر الجملة يصبح أول سطر). صار اللف منطقياً قبل التحويل عبر `arabic_support.wrap`.
- اختبار جديد يتحقق أن كل محرف في الواجهة وبنك الأسئلة له رسم فعلي في الخط، فلا تظهر مربّعات فارغة.
- ملفا `LICENSE` و`CHANGELOG.md`، وبناء إصدار موقَّع اختياري عبر أسرار المستودع.

## 1.1

- **دعم العربية:** خط Kivy الافتراضي بلا حروف عربية، و`SDL_ttf` لا ينفّذ تشكيل OpenType ولا خوارزمية BiDi، فكانت كل نصوص التطبيق تظهر مربّعات فارغة. أُضيف خط Amiri وسُجّل كخط افتراضي، وكُتب `arabic_support.py`: جدول أشكال سياقية مبني من `unicodedata`، ليجاتورات لام‑ألف، ونسخة مختصرة من خوارزمية BiDi (W4/W5/N1/N2) تحافظ على ترتيب الأرقام والنِّسب والنص اللاتيني.
- إعادة بناء الواجهة على `ScreenManager` بسبع شاشات.
- تخزين JSON لبنك الأسئلة والإعدادات وسجل النتائج، مع ترحيل تلقائي للصيغة النصية القديمة.
- تصحيح فوري اختياري، تنقّل بين الأسئلة، شريط تقدّم، ومؤقّت قابل للضبط مع إنهاء تلقائي.
- شاشة نتيجة بمراجعة كل إجابة خاطئة، وسجل لآخر 50 محاولة.
- إدارة الأسئلة: إضافة وتعديل وحذف.
- دعم زر الرجوع في أندرويد، أيقونة وشاشة بدء، وإزالة الأذونات غير اللازمة.
- اختبارات تعمل بلا Kivy عبر بدائل خفيفة، تُشغَّل في CI قبل كل بناء.
- نشر تلقائي على GitHub Releases عند دفع وسم `v*`.

## 1.0

الإصدار الأول: ملف `main.py` واحد، أسئلة في ملف نصي، مؤقّت ثابت 10 دقائق.

</div>
